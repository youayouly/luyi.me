---
description: Luke 站点正文与工程样式约定（写作结构、lk- 前缀、布局约束）
globs: docs/**/*
alwaysApply: false
---

# Luke Blog System Rules

## 输出前自检

通读本规则；任一条不满足则**整段重写**，不要局部修补糊弄。

## Writing（正文）

- **语言**：中文为主；技术名词、API、类名、路径保留英文。
- **版式**：每段最多 **3 行**（换行即新段）。
- **结构**：每议题用 **问题 → 原因 → 解决**（可小标题，顺序不可乱）。
- **禁止**：泛泛定义、教科书式背景、与当前仓库无关的抽象例子。
- **必须**：点名本工程中的路径、组件、配置或真实行为（可 `docs/`、`docs/.vuepress/`、`scripts/`）。

## Style（语气）

- 简洁；工程化；**类笔记**（短句、可扫读，不堆形容词）。

## UI / Code（站点实现）

- 自定义类名、CSS 变量前缀：**`lk-`**。
- **禁止**为省事滥用 `!important`；若覆盖 Hope/VuePress，提高选择器特异度或 `:has()`，与现有 `index.scss` 一致。
- **禁止**用 `100vw` 等把整页或主栅格撑破侧栏/中轴；放宽宽度用 `min()`、`max-width` 或主题已有变量。
- **修改样式**时保持版心/主列 **中轴对齐**（勿只改一侧留白导致视觉偏心）。

## 示例（正文段落 — 合格）

**问题**：构建后 About 推荐区过宽。  
**原因**：主列未设 `max-width`，大屏下与侧栏比例失调。  
**解决**：在 `docs/.vuepress/styles/index.scss` 为 `lk-about-v2-main__col--main` 收紧 `max-width` 并 `justify-self: start`。

## 示例（反例）

❌ 「VuePress 是一个静态站点生成器，它支持 Markdown……」（无工程锚点、无 PRS）  
❌ 一段 8 行说明同一小事。
